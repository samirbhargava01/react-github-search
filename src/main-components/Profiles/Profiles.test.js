import { render } from '@testing-library/react';
import { Profiles } from './Profiles';
 
describe('Profiles', () => {
  test('renders Profiles component', () => {
    render(<Profiles />);
  });
});
